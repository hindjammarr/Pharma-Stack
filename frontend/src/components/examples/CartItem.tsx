import { CartItem } from "../CartItem";\nimport { useState } from "react";\n\nexport default function CartItemExample() {\n  // todo: remove mock functionality\n  const [quantity, setQuantity] = useState(2);\n  \n  const mockCartItem = {\n    id: \"1\",\n    name: \"Doliprane 1000mg - Boîte de 20 comprimés\",\n    price: 4.50,\n    quantity,\n    maxStock: 10,\n    onQuantityChange: (id: string, newQuantity: number) => {\n      console.log(`Updating quantity for ${id} to ${newQuantity}`);\n      setQuantity(newQuantity);\n    },\n    onRemove: (id: string) => console.log(`Removing item ${id}`)\n  };\n  \n  return (\n    <div className="w-full max-w-2xl">\n      <CartItem {...mockCartItem} />\n    </div>\n  );\n}